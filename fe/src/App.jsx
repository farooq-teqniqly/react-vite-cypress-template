/**
 * A React functional component that renders a simple "Hello React!" message.
 *
 * This component does not accept any props and serves as a basic example
 * of rendering a React component using JSX.
 *
 * @returns {JSX.Element} A div element containing the text "Hello React!".
 */
const App = () => {
  return <div>Hello React!</div>;
};

export default App;
